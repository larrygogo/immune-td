# Immune TD 前端环境变量（可选）

# 后端 API 基地址；留空时走相对路径 /api（配合 vite 开发服 proxy 或同源部署）
# dev：留空（Vite 通过 proxy 转 localhost:3100）
# prod：同源部署留空；跨域时填 https://api.example.com
VITE_API_BASE_URL=

# 同域名不同端口部署时，设端口即可自动拼接 http(s)://当前域名:端口
# 例：前后端同域名，前端 80/443，后端 3100 → 设 VITE_API_PORT=3100
# 优先级：VITE_API_BASE_URL > VITE_API_PORT > 相对路径
VITE_API_PORT=
